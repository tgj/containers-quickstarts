#!/bin/busybox sh

# the | true suppresses the error message if the directory is empty
cp $S2I_DESTINATION/src/local/* /drouter/local | true
cp $S2I_DESTINATION/src/config/* /drouter/config | true

exit $rc
